services:
  - type: worker
    name: neirostat-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python neirobot.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: DEEPSEEK_KEY
        sync: false
      - key: OPENROUTER_KEY
        sync: false
